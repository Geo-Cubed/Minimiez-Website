<div class="card @this.ClassStyle" style="@this.ExtraStyles">
    <div class="card-header" style="cursor:pointer;" @onclick="this.ToggleCollapsed">
        <div class="d-flex flex-row">
            <div><!--So the grow centers the headder :)--></div>
            <div class="flex-grow-1">
                @this.Header
            </div>
            @if (this.IsCollapsed)
            {
                <span class="oi oi-chevron-right pr-2 pt-2"></span>
            }
            else
            {
                <span class="oi oi-chevron-bottom pr-2 pt-2"></span>
            }
        </div>
    </div>


    @if (this.Body != null)
    {
        <div class="card-body @this.CollapsedStyle">
            @this.Body
        </div>
    }

    @if (this.Footer != null)
    {
        <div class="card-footer @this.CollapsedStyle">
            @this.Footer
        </div>
    }
</div>
